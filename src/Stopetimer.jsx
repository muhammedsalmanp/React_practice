import React, { useState } from 'react'
import { use } from 'react'

const Stopetimer = () => {
    const [time,setTime] = useState(0);
    const [isRenning , setIsrenning] = useState(false);
    
  return (
    <div>
      
    </div>
  )
}

export default Stopetimer
